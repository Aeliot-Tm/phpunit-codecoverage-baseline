version: '3.1'

services:
  php:
    build:
      context: ./
      dockerfile: .infra/docker/php/Dockerfile
    expose:
      - '9003'
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: on-failure
    stdin_open: true
    tty: true
    volumes:
      - ./:/app:delegated

