name: Prepare Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 2.2.0 or v2.2.0)'
        required: true
        type: string

jobs:
  validate-and-prepare:
    name: Validate and Prepare Release
    runs-on: ubuntu-latest

    permissions:
      contents: write

    outputs:
      version: ${{ steps.set_version.outputs.version }}

    steps:
      - name: Check branch
        run: |
          if [ "${{ github.ref }}" != "refs/heads/main" ]; then
            echo "❌ This workflow must be run on the main branch"
            exit 1
          fi

      - name: Validate version format
        id: validate
        run: |
          VERSION="${{ github.event.inputs.version }}"

          if ! echo "$VERSION" | grep -qE '^[a-zA-Z0-9][a-zA-Z0-9._-]*[a-zA-Z0-9]$'; then
            echo "❌ Invalid version format: $VERSION"
            echo ""
            echo "Version must:"
            echo "  - Start with a letter or digit"
            echo "  - End with a letter or digit"
            echo "  - Contain only: letters (a-z, A-Z), digits (0-9), dots (.), dashes (-), underscores (_)"
            echo ""
            echo "Valid examples: 2.2.0, v2.2.0, 1.0.0-beta, 2024.01.15"
            exit 1
          fi

      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_RELEASE_TOKEN }}

      - name: Check if tag exists
        run: |
          VERSION="${{ github.event.inputs.version }}"
          if git ls-remote --tags origin | grep -q "refs/tags/${VERSION}$"; then
            echo "❌ Tag ${VERSION} already exists!"
            exit 1
          fi
          echo "✅ Tag ${VERSION} does not exist yet"

      - name: Set version output
        id: set_version
        run: |
          VERSION="${{ github.event.inputs.version }}"
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "Version set to: ${VERSION}"

      - name: Update VERSION constant
        run: |
          VERSION="${{ github.event.inputs.version }}"
          sed -i "s/const VERSION = '[^']*'/const VERSION = '${VERSION}'/" src/Console/Application.php

          echo "✅ Updated Application.php VERSION constant to: ${VERSION}"
          grep "const VERSION" src/Console/Application.php

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and push version bump
        run: |
          VERSION="${{ github.event.inputs.version }}"
          git add src/Console/Application.php
          if ! git diff --staged --quiet; then
            git commit -m "chore: bump version to ${VERSION}"
            git push origin main
            echo "✅ Committed and pushed version bump"
          else
            echo "✅ Version already set in Application.php, no commit needed"
          fi

      - name: Create and push tag
        run: |
          VERSION="${{ github.event.inputs.version }}"
          git tag "${VERSION}"
          git push origin "${VERSION}"
          echo "✅ Created and pushed tag: ${VERSION}"

  create-draft-release:
    name: Create Draft Release
    needs: validate-and-prepare
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Create draft release
        uses: softprops/action-gh-release@v3
        with:
          token: ${{ secrets.GH_RELEASE_TOKEN }}
          tag_name: ${{ needs.validate-and-prepare.outputs.version }}
          name: Release ${{ needs.validate-and-prepare.outputs.version }}
          draft: true
          generate_release_notes: true

  build-phar:
    name: Build and Sign PHAR
    needs: [validate-and-prepare, create-draft-release]
    runs-on: ubuntu-latest

    env:
      PCCB_PHAR: pccb.phar
      PCCB_SIGN: pccb.phar.asc

    permissions:
      artifact-metadata: write
      attestations: write
      contents: write
      id-token: write

    steps:
      - name: Checkout tag
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.validate-and-prepare.outputs.version }}
          fetch-depth: 0

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.2
          ini-values: phar.readonly=0
          tools: composer, box
          coverage: none

      - name: Set versions
        run: cp composer.7.0.lock composer.lock

      - name: Cache Composer packages
        id: composer-cache
        uses: actions/cache@v5
        with:
          path: vendor
          key: "os-${{ runner.os }}-php-${{ runner.php-version }}-composer-${{ hashFiles('**/composer.lock') }}"
          restore-keys: "os-${{ runner.os }}-php-${{ runner.php-version }}-composer-"

      - name: Install Composer dependencies
        uses: ramsey/composer-install@v4
        with:
          composer-options: '--no-dev'

      - name: Build PHAR
        run: composer build

      - name: Ensure the PHAR works
        run: php ${{ env.PCCB_PHAR }} list

      - name: Import GPG key
        uses: crazy-max/ghaction-import-gpg@v7
        with:
          gpg_private_key: ${{ secrets.GPG_KEY_A9B00724C9F9727D799CDEB4721E74FEB9C5DFE7 }}
          passphrase: "${{ secrets.GPG_KEY_A9B00724C9F9727D799CDEB4721E74FEB9C5DFE7_PASSPHRASE }}"

      - name: Sign the PHAR
        run: |
          gpg --local-user A9B00724C9F9727D799CDEB4721E74FEB9C5DFE7 \
              --batch \
              --yes \
              --passphrase="${{ secrets.GPG_KEY_A9B00724C9F9727D799CDEB4721E74FEB9C5DFE7_PASSPHRASE }}" \
              --detach-sign \
              --output ${{ env.PCCB_SIGN }} \
              ${{ env.PCCB_PHAR }}

      - name: Verify signature
        run: gpg --verify ${{ env.PCCB_SIGN }} ${{ env.PCCB_PHAR }}

      - name: Upload PHAR artifact
        uses: actions/upload-artifact@v7
        id: upload_phar
        with:
          name: ${{ env.PCCB_PHAR }}
          path: ${{ env.PCCB_PHAR }}

      - name: Upload signature artifact
        uses: actions/upload-artifact@v7
        id: upload_sign
        with:
          name: ${{ env.PCCB_SIGN }}
          path: ${{ env.PCCB_SIGN }}

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.PCCB_PHAR }}
          subject-digest: sha256:${{ steps.upload_phar.outputs.artifact-digest }}

      - name: Attach PHAR and signature to draft release
        uses: softprops/action-gh-release@v3
        with:
          token: ${{ secrets.GH_RELEASE_TOKEN }}
          tag_name: ${{ needs.validate-and-prepare.outputs.version }}
          files: |
            ${{ env.PCCB_PHAR }}
            ${{ env.PCCB_SIGN }}
